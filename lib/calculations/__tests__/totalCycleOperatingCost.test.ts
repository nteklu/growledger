import { describe, it } from "vitest";

describe("totalCycleOperatingCost", () => {
  it.todo(
    "sums all cost items assigned to the given cycle_crop (FR-09)",
  );
  it.todo(
    "sums direct and estimated allocation_type costs together, never blending them silently",
  );
  it.todo(
    "excludes cost items that belong to a different cycle_crop in the same cycle",
  );
});
