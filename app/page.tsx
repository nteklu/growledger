/**
 * Screen 1 · Landing / sign-in — Access (FR-01)
 * Required content: value proposition, sign-in, sign-up.
 */
export default function LandingPage() {
  return (
    <main>
      <h1>GrowLedger</h1>
      <p>
        Understand how operating cost and saleable yield change from one
        comparable crop cycle to the next.
      </p>
      {/* TODO: sign-in / sign-up forms (Supabase Auth, FR-01) */}
    </main>
  );
}
