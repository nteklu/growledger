/**
 * Screen 3 · Crop profiles — Comparison setup (FR-03)
 * Required content: crop_name, cultivar, standard_yield_unit list and form.
 */
export default function CropProfilesPage() {
  return (
    <main>
      <h1>Crop profiles</h1>
      {/* TODO: list — crop_name, cultivar, standard_yield_unit */}
      {/* TODO: add crop profile form */}
    </main>
  );
}
