/**
 * Screen 2 · Dashboard — Overview (FR-13)
 * Required content: summary cards, trend chart, category breakdown,
 * flags, recent cycles, and empty states.
 */
export default function DashboardPage() {
  return (
    <main>
      <h1>Dashboard</h1>
      {/* TODO: summary cards — cost/saleable unit, active cycles, open flags */}
      {/* TODO: trend chart — cost per unit across completed cycles (Recharts) */}
      {/* TODO: flagged cycles list */}
      {/* TODO: recent cycles table */}
    </main>
  );
}
