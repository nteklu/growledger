/**
 * Screen 5 · Cycle list — Find records (FR-05)
 * One row per cycle_crop, so a two-crop cycle lists as two rows.
 * Required content: search/filter by crop, status, date range;
 * crop, status, start-end dates, cost/unit.
 */
export default function CyclesPage() {
  return (
    <main>
      <h1>Cycles</h1>
      {/* TODO: search / filter — crop, status, date range */}
      {/* TODO: table — one row per cycle_crop */}
    </main>
  );
}
