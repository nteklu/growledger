/**
 * Screen 4 · Grow spaces — Context (FR-04)
 * Required content: name, area (floor footprint), area_unit (fixed),
 * production_method (fixed) list and form.
 */
export default function GrowSpacesPage() {
  return (
    <main>
      <h1>Grow spaces</h1>
      {/* TODO: list — name, area, production_method */}
      {/* TODO: add grow space form (area_unit and production_method are fixed lists) */}
    </main>
  );
}
